# ==============================================
# Waggin' Meals Environment Variables Template
# ==============================================
#
# IMPORTANT: This file is a template.
# Copy this to .env.local and fill in your actual values.
# NEVER commit .env.local to git (it's in .gitignore)
#
# For production deployment (Netlify/Vercel):
# Add these variables to your hosting platform's environment variable dashboard
#
# ==============================================

# ----------------------------------------------
# Site Configuration
# ----------------------------------------------
NEXT_PUBLIC_SITE_URL=https://wagginmeals.com


# ----------------------------------------------
# Supabase Database
# ----------------------------------------------
# Get these from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key


# ----------------------------------------------
# Admin Authentication
# ----------------------------------------------
# Set your own admin credentials for /admin access
ADMIN_USERNAME=your_admin_username

# IMPORTANT: Password must be a bcrypt hash, NOT plain text
# To generate a hash, run:
#   node scripts/generate-admin-password-hash.js YourPasswordHere
# Then copy the output hash here
ADMIN_PASSWORD_HASH=your_bcrypt_password_hash_here

# LEGACY: Plain text password (NOT RECOMMENDED - use ADMIN_PASSWORD_HASH instead)
# Only uncomment if you need backward compatibility
# ADMIN_PASSWORD=your_secure_admin_password

# Generate a random 32+ character string for session encryption
# Example: openssl rand -base64 32
SESSION_SECRET=your_session_secret_min_32_characters


# ----------------------------------------------
# Customer Authentication
# ----------------------------------------------
# JWT secret for customer magic link authentication
# Generate a random 32+ character string
# Example: openssl rand -base64 32
CUSTOMER_JWT_SECRET=your_customer_jwt_secret_min_32_characters


# ----------------------------------------------
# Authorize.net Payment Gateway
# ----------------------------------------------
# Get these from: https://account.authorize.net/
# Use "sandbox" for testing, "production" for live payments

# Environment: sandbox or production
AUTHORIZENET_ENVIRONMENT=sandbox
AUTHORIZE_NET_ENVIRONMENT=sandbox
NEXT_PUBLIC_AUTHORIZENET_ENVIRONMENT=sandbox

# API Credentials
AUTHORIZENET_API_LOGIN_ID=your_api_login_id
AUTHORIZE_NET_API_LOGIN_ID=your_api_login_id
NEXT_PUBLIC_AUTHORIZENET_API_LOGIN_ID=your_api_login_id

AUTHORIZENET_TRANSACTION_KEY=your_transaction_key
AUTHORIZE_NET_TRANSACTION_KEY=your_transaction_key

# Public Client Key for Accept.js (browser-side tokenization)
AUTHORIZENET_PUBLIC_CLIENT_KEY=your_public_client_key
AUTHORIZE_NET_CLIENT_KEY=your_public_client_key
NEXT_PUBLIC_AUTHORIZENET_PUBLIC_CLIENT_KEY=your_public_client_key
NEXT_PUBLIC_AUTHORIZE_NET_CLIENT_KEY=your_public_client_key


# ----------------------------------------------
# Email Configuration (SMTP)
# ----------------------------------------------
# For Gmail: Use App Password (not regular password)
# https://support.google.com/accounts/answer/185833

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_gmail_app_password
EMAIL_FROM=noreply@wagginmeals.com


# ----------------------------------------------
# GoHighLevel CRM Integration
# ----------------------------------------------
# Get these from: https://app.gohighlevel.com/settings/integrations
# REQUIRED: GHL_API_KEY and GHL_LOCATION_ID must be set for CRM sync to work
# OPTIONAL: Custom field IDs below (if not set, contact will sync without custom data)

GHL_ENABLED=true
GHL_API_KEY=your_ghl_api_key
GHL_LOCATION_ID=your_ghl_location_id

# Custom Field IDs (Optional - get from GHL Custom Fields settings)
# These are the actual field IDs generated by GoHighLevel, not logical names
# Example: GHL_CUSTOM_FIELD_CONSULTATION_ID=abc123xyz456
GHL_CUSTOM_FIELD_CONSULTATION_ID=your_consultation_id_field_id
GHL_CUSTOM_FIELD_PET_COUNT=your_pet_count_field_id
GHL_CUSTOM_FIELD_SPENDING=your_spending_field_id
GHL_CUSTOM_FIELD_FREQUENCY=your_frequency_field_id


# ==============================================
# DEPLOYMENT NOTES
# ==============================================
#
# Local Development:
# 1. Copy this file to .env.local
# 2. Fill in all values
# 3. Run: npm run dev
#
# Production (Netlify):
# 1. Go to: Site Settings > Environment Variables
# 2. Add each variable above (except comments)
# 3. Mark sensitive vars as "encrypted" (API keys, passwords, secrets)
#
# Security Checklist:
# ✓ Never commit .env.local to git
# ✓ Use different credentials for sandbox vs production
# ✓ Generate strong SESSION_SECRET (32+ random characters)
# ✓ Enable 2FA on all service accounts
# ✓ Regularly rotate API keys
# ✓ Use environment-specific Authorize.net accounts
#
# ==============================================
